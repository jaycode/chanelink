require 'test_helper'

class AgodaChannelTest < ActiveSupport::TestCase
  test "Connection to Agoda server" do
    mapping_params = {
      :channel_id => channels(:ctrip),
      :room_type_id =>
    }
  end
end