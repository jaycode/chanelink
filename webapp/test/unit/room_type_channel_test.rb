require 'test_helper'

class RoomTypeChannelTest < ActiveSupport::TestCase
  test "Room type channel creation" do
    mapping_params = {
      :channel_id => channels(:ctrip),
      :room_type_id =>
    }
  end
end