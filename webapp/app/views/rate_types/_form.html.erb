<%= form_for @rate_type do |f| %>
  <p>
    <label><%= t('rate_types.new.label.name') %><%= required %></label>
    <%= f.text_field :name %>
  </p>
  <br/>
  <p>
    <%= f.submit t("button.save") %>
    <%= link_to t('button.cancel'), rate_types_path %>
  </p>
<% end %>